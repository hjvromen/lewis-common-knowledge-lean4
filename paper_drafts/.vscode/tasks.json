{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Clean with XeLaTeX",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "xelatex -synctex=1 -interaction=nonstopmode -file-line-error -output-directory=out main.tex && biber out/main && xelatex -output-directory=out main.tex && xelatex -output-directory=out main.tex && latexmk -c -outdir=out"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
